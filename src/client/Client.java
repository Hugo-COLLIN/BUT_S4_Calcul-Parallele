package client;

public class Client {
    public void calculer(String scenePath, int nbPart, int taille){
        double nbLongPart = Math.sqrt(nbPart);
        double partLength = taille/nbLongPart;
        for (int i=0; i<nbLongPart; i++){
            for (int y=0; y<nbLongPart; y++){

            }
        }
    }
}
